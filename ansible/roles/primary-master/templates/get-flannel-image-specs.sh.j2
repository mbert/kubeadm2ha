#!/bin/sh

errorExit() {
	echo "*** $*" 1>&2
	exit 1
}

wget -q -O /tmp/flannel.yaml '{{ FLANNEL_YAML_URL }}'

egrep '^[ \t]*image:' /tmp/flannel.yaml  | sed 's/.*image: *//' | sort -u

rm -f /tmp/flannel.yaml
